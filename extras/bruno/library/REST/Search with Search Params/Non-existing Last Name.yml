info:
  name: Non-existing Last Name
  type: http
  seq: 4

http:
  method: GET
  url: "{{restUrl}}?last_name=Xxx"
  headers:
    - name: Accept
      value: application/json
  params:
    - name: last_name
      value: Xxx
      type: query
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "404"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
